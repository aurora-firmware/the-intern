# CLI Reference

<!-- This page is populated by the mdbook preprocessor or pre-build step introduced by T-082. -->
<!-- Stub only — do not add content here manually. -->

```mermaid
graph TD;
    A[User] --> B[The Intern CLI];
```
