[package]
name = "requests-handler"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "sync", "net", "signal", "macros", "time"] }
bob-core = { path = "../bob-core" }
chrono = "0.4"
tracing = "0.1"
async-trait = { workspace = true }
