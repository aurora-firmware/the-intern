name: Build

on:
  pull_request:
  push:
    branches:
      - dev-agent
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder build step
        run: echo "build placeholder"
