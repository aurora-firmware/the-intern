name: Build

on:
  pull_request:
  push:
    branches:
      - dev-agent
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Placeholder build step
        run: echo "build placeholder"
