name: Test

on:
  pull_request:
  push:
    branches:
      - dev-agent
      - main

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Placeholder test step
        run: echo "test placeholder"
