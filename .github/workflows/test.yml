name: Test

on:
  pull_request:
  push:
    branches:
      - dev-agent
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder test step
        run: echo "test placeholder"
